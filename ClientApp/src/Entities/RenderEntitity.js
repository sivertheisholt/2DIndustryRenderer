class RenderEntity {
  constructor(id, name, x, y, scale, path) {
    this.id = id;
    this.name = name;
    this.x = x;
    this.y = y;
    this.scale = scale;
    this.path = path;
  }
}

export default RenderEntity;
