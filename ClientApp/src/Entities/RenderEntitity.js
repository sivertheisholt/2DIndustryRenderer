class RenderEntity {
  constructor(id, name, x, y, scale, path, type) {
    this.id = id;
    this.name = name;
    this.x = x;
    this.y = y;
    this.scale = scale;
    this.path = path;
    this.type = type;
  }
}

export default RenderEntity;
